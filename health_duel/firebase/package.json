{
  "name": "health-duel-firestore-rules-tests",
  "private": true,
  "description": "Emulator tests for ../firestore.rules — the only thing that can prove the security model of a client-only app. Run via `firebase emulators:exec` from health_duel/ (see README.md in this directory).",
  "type": "commonjs",
  "scripts": {
    "test": "mocha test/**/*.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "mocha": "^10.7.3"
  }
}
